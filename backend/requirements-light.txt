# Minimal requirements - OHNE schwere Dependencies
# Kompiliert in ~2 Minuten auf Pi Zero 2 W

fastapi==0.109.0
uvicorn==0.27.0
websockets==12.0
aiosqlite==0.19.0

# KEINE Pydantic - verwenden stattdessen Python Dicts
# Funktioniert genauso, nur ohne automatische Validierung
